import asyncHandler from "../../../src/utils/asyncHandler.js";

const createPost = asyncHandler(async (req, res , next) => {
    // check if the post is already exists or not by checking the model for the same person
    // TODO
     
    // create the post
    
}); 